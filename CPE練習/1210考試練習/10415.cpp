#include<bits/stdc++.h>
using namespace std;

void calculate_press(int finger_press_times[ ], bool isPressed[ ], char key) {
    if (key == 'c' || key == 'd' || key == 'e' || key == 'f' || key == 'g') {
        
    } 
}

int main( ) {
    ios :: sync_with_stdio(0);
    cin.tie(0);

    bool isPressrd[10] = {false};
    int finger_press_times[10] = {0};
    int test_cases;
    cin >> test_cases;
    while(test_cases--) {
        string input;
        getline(cin >> ws, input);
        for (int i = 0 ; i < input.length( ) ; i++) {
            
        }
    }
}